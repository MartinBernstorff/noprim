# Not a distribution; exists for tach check-external. See AGENTS.md.
[project]
name = "noprim-core"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2", "iterpy>=1.15", "pathspec>=0.12", "typing-extensions>=4.12"]
