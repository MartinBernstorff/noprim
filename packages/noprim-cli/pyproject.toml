# Not a distribution; the root pyproject.toml builds the only wheel. This
# manifest exists so `tach check-external` can enforce per-module dependencies.
[project]
name = "noprim-cli"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = ["typer>=0.15", "pydantic>=2.11"]
